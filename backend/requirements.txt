# ── Linux 服务器 CPU-only 安装指引 ──────────────────────────
# PyTorch CPU-only（避免下载 2GB+ CUDA 包）:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# TensorFlow CPU-only:
#   pip install tensorflow-cpu>=2.10.0
# ───────────────────────────────────────────────────────────
torch>=2.0.0
tensorflow>=2.10.0
numpy>=1.24.0
pandas>=2.0.0
baostock>=0.8.8
scikit-learn>=1.2.0
matplotlib>=3.7.0
tushare>=1.4.0

# Web 后端
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.2.0
pydantic>=2.5.0
redis>=5.0.0
httpx>=0.27.0
# ---- 登录/鉴权与审计（客户端可追溯操作历史）----
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
